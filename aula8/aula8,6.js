let nome = 'emanuel'
let tipoJogo = 'IN'
let etapaJogo = 'FI'
let categoria = 1
let qntdIngressos = 3
let preço 

switch (etapaJogo) {
    case 'SF':
        switch (categoria) {
            case 1:
                preço === 200
    break
            case 2:
                 preço === 150
    break
            case 3:
                preço === 100
    break
            case 4:
                preço === 75
    default:
        console.log('categoria invalida')


        }
    break

    case 'DT':
        switch (categoria) {
            case 1:
                preço === 200
    break
            case 2:
                 preço === 150
    break
            case 3:
                preço === 100
    break
            case 4:
                preço === 75
    default:
        console.log('categoria invalida')
}
    break

    case 'FI':
        switch (categoria) {
            case 1:
                preço === 200
    break
            case 2:
                 preço === 150
    break
            case 3:
                preço === 100
    break
            case 4:
                preço === 75
    default:
        console.log('categoria invalida')

        